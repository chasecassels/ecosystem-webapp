
Simple web application allowing search, sort and crud operations on a database of canadian ecosystem data. 
Uses Tomcat connection pooling, PostgreSQL, a Java backend with servlets, and a dynamic JSP and CSS frontend.

